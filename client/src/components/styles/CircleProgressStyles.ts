import { StyleSheet } from 'react-native';

// Style definitions for the CircleProgress component container layout
export const CircleProgressStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});
